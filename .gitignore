.DS_Store
logs/
.run/
/data/

# Local env with real paths/secrets — never commit
deploy/.env
.env

# goal-experiment scratch scripts (kept locally, not committed)
server/scratch/
.codegraph/
cockpit-astryx/node_modules/
telegram.session*
